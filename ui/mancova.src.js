'use strict';

var options = require("./mancova.options");

var layout = ui.extend({

    label: "MANCOVA",
    type: "root",
    stage: 2,
    controls: [

    ],

    actions: [

    ]
});

module.exports = { ui : layout, options: options };
