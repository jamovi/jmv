
'use strict';

var options = require("./gofit.options");

var layout = ui.extend({

    label: "GoFit",
    type: "root",
    stage: 0,
    controls: [

    ],

    actions: [

    ]
});

module.exports = { ui : layout, options: options };
