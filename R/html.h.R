
# This file is automatically generated, you probably don't want to edit this

htmlOptions <- if (requireNamespace('jmvcore')) R6::R6Class(
    "htmlOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            content = "<p>the <em>fish</em> was <strong>delish</strong></p>", ...) {

            super$initialize(
                package='jmv',
                name='html',
                requiresData=FALSE,
                ...)

            private$..content <- jmvcore::OptionString$new(
                "content",
                content,
                default="<p>the <em>fish</em> was <strong>delish</strong></p>")

            self$.addOption(private$..content)
        }),
    active = list(
        content = function() private$..content$value),
    private = list(
        ..content = NA)
)

htmlResults <- if (requireNamespace('jmvcore')) R6::R6Class(
    inherit = jmvcore::Group,
    active = list(
        html = function() private$.items[["html"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="HTML")
            self$add(jmvcore::Html$new(
                options=options,
                name="html",
                title=""))}))

htmlBase <- if (requireNamespace('jmvcore')) R6::R6Class(
    "htmlBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = 'jmv',
                name = 'html',
                version = c(1,0,0),
                options = options,
                results = htmlResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE)
        }))
